<h1><%= @cocktail.name %></h1>

<p>Doses:</p>

<ul>
  <% @cocktail.doses.each do |dose| %>
  <li><%= dose.description %> <%= dose.ingredient.name %></li>
  <%= link_to "Delete a dose", dose_path(dose), method: :delete, data: { confirm: "Delete this dose?"} %>
  <% end %>
</ul>

<%= link_to "Add a new dose", new_cocktail_dose_path(@cocktail) %>

<%= link_to "Delete a cocktail", cocktail_path(@cocktail), method: :delete, data: { confirm: "Delete this cocktail?"} %>


<%= link_to "Go back to cocktail list", cocktails_path %>

